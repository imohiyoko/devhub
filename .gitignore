# 個人設定（各自の環境に依存するため除外）
settings/config.json
settings/server.json

# macOS
.DS_Store

# ログ
*.log
settings/envs.json
settings/config.json
settings/launches.json

# SQLite
*.db
*.db-wal
*.db-shm

# Go
/dist/
/devhub
/devhub.exe
*.test

# mise（個人のローカル上書き。共有する .mise.toml は追跡する）
.mise.local.toml
mise.local.toml
.mise/
