# devhub 作業ルール（Claude 向け）

## 依存の供給網チェックと棚卸しは `/dep-audit` スキルで行う

サードパーティ依存を追加・更新するとき（Go モジュール、および `shared/vendor/` に手動同梱する JS/CSS blob）は、`/dep-audit` スキルの供給網チェック（出所が公式か・バージョン固定・SHA-256 で改ざん検知・既知脆弱性確認・由来の記録）を通す。

固定した依存はセキュリティ修正が自動では降ってこず、手動同梱の blob は Dependabot の対象外。台帳 `dep-audit.json` に固定版と最終確認日を持ち、`/dep-audit` が最新安定版と突き合わせて、新版あり or 最終確認から一定日数経過のどちらかで棚卸しレポートを要求する。定期的に `/dep-audit` を走らせること。
